# ── Secrets / private (NEVER upload) ───────────────────────────
.kaggle/
.kaggle2/
private/
**/kaggle.json
**/credentials.json
**/*api*key*
**/*.token

# ── Official competition materials (available on the Kaggle site) ─
docs/official/

# ── Local engine copied into agents for testing (official, large) ─
**/cg/
agents/**/cg/

# ── Build artifacts / environments / caches ────────────────────
venv/
**/submission.tar.gz
__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/
/tmp/

# Private multi-account tooling
tools/kaggle-as.sh

# Pinned opponent decks cached by tools/cabt_eval.py (machine-local test fixtures)
tools/.opp_cache_*.csv

# Card images rendered from the official PDF (copyright — local only)
web/card_imgs/

# AtomCode config (machine-specific)
.atomcode/
